import { useEffect, useState } from 'react';
import { motion } from 'framer-motion';
import {
	SiGithub,
	SiTwitter,
} from 'react-icons/si';
import { Link, useLocation } from 'react-router-dom';
import styled from 'styled-components';

const How = (): JSX.Element  => {
	const { pathname } = useLocation();

	return (
		<>
			<p>hi</p>
		</>
	);
};


export default How;